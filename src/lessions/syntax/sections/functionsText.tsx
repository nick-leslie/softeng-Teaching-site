import SectionTitle from "@/components/SectionTitle";

export default function FunctionsText() {
  return (
    <div className="flex flex-col place-self-center gap-5">
      <SectionTitle title={"Part 2:Functions"} />
      <div>
        After Variables, functions are the most basic building blocks of
        programs. Functions bundle logic together into a reusable component.
        Lets take a look at the basic syntax in each language.
      </div>
    </div>
  );
}
